# PostgreSQL Database Connection URL - Replace with your actual connection string in the real .env file
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME"

# Optional: Define the port the server should run on (defaults to 3001 if not set)
# PORT=3001